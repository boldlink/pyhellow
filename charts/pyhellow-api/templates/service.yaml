apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyhellow-api.fullname" . }}
  labels:
    {{- include "pyhellow-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyhellow-api.selectorLabels" . | nindent 4 }}
